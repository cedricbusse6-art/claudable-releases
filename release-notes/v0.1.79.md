# Claudable Launcher v0.1.79

fix: black screen after login - wait for fade transition before unmounting LoginScreen

- LoginScreen: add transitionend listener for fade animation completion
- LoginScreen: new onFadeComplete prop fires when opacity transition ends
- App.tsx: wait for fadeComplete before setting gated=false
- Prevents black screen gap between LoginScreen unmount and main app mount

# Claudable Launcher v0.1.77

feat: Light Version category + Android download; CSP fixes; build-time version fallback

- Add Light Version / Mobile category in Settings with Android APK download button
- Fix CSP to allow all external API domains (GitHub, HD2, Minecraft, Steam RSS, etc.)
- Inject __APP_VERSION__ at build time for reliable version display
- Add error logging for getAppInfo IPC failures
- Release notes fetch now logs HTTP errors instead of silent fail
